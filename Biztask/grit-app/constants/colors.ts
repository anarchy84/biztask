// 한글 주석: GRIT V2 컬러 토큰 (2026-04-28 갈아엎음)
//
// ▣ 설계 원칙 (디자인 핸드오프 README 기반):
//   1) 다크모드 우선 (라이트는 v2)
//   2) 그린 4단 위계 (단톤 안에서도 강도 분리 → 시선 우선순위 확보)
//   3) 베이스 컬러는 #000 아닌 #121212 (해커 터미널 톤 회피)
//   4) 본문은 순백 #FFF 아닌 오프화이트 #E4E4E7 (눈 부심 회피)
//   5) Semantic 컬러는 의미 정보에만 사용 (좋아요=빨강, 인증=파랑, 경고=주황)
//
// ▣ 컬러 사용 위계:
//   - brand.500: 메인 (CTA, 활성 탭, 팔로우 버튼)
//   - brand.400: 강조 (숫자 게이지, 시선 끌기)
//   - brand.300: 글로우 (떠오르는 강조, opacity 60% 권장)
//   - brand.800: 가이드 (게이지 트랙, 미세 라인)

// ─────────────────────────────────────────────
// 1. Brand - Emerald 4단 (단톤 위계)
// ─────────────────────────────────────────────

export const brand = {
  300: '#34D399',   // 글로우 / 호버
  400: '#10B981',   // 강조: 숫자 게이지, 시선 끌기
  500: '#059669',   // ★ PRIMARY - CTA, 활성 탭, 팔로우 버튼
  600: '#047857',   // Pressed 상태
  700: '#065F46',
  800: '#064E3B',   // 게이지 트랙, 미세 라인
} as const

// ─────────────────────────────────────────────
// 2. Background - 다크 4단 (#121212부터, 순흑 X)
// ─────────────────────────────────────────────

export const bg = {
  base:    '#121212',   // 앱 전체 배경
  surface: '#1A1A1A',   // 카드 표면
  raised:  '#242424',   // 호버/액티브 카드
  nested:  '#2D2D2D',   // 중첩 카드 (시크릿 카테고리 카드 안 등)
} as const

// ─────────────────────────────────────────────
// 3. Text - 오프화이트 4단 (순백 #FFF 사용 금지)
// ─────────────────────────────────────────────

export const text = {
  primary:   '#E4E4E7',   // 본문 (메인 텍스트)
  secondary: '#A1A1AA',   // 메타데이터, 시간
  tertiary:  '#71717A',   // 캡션, 비활성
  disabled:  '#52525B',   // 디스에이블
} as const

// ─────────────────────────────────────────────
// 4. Line - hairline border 3단 (rgba 화이트)
// ─────────────────────────────────────────────

export const line = {
  subtle:  'rgba(255,255,255,0.04)',
  default: 'rgba(255,255,255,0.06)',   // 기본 카드 보더
  strong:  'rgba(255,255,255,0.10)',
} as const

// ─────────────────────────────────────────────
// 5. Semantic - 의미 컬러 (전용 용도만)
// ─────────────────────────────────────────────

export const semantic = {
  like:     '#EF4444',   // 좋아요/하트 전용
  verify:   '#3B82F6',   // 인증 뱃지 전용 (파란딱지)
  warn:     '#F59E0B',   // 경고
  danger:   '#DC2626',   // 위험 (삭제 등)
} as const

// ─────────────────────────────────────────────
// 6. CTA Button - emerald 위 텍스트 (순백/순흑 X)
// ─────────────────────────────────────────────

export const onBrand = '#ECFDF5'   // 민트 화이트 (CTA 버튼 위 텍스트)

// ─────────────────────────────────────────────
// 7. 통합 export (RN StyleSheet에서 import 편하게)
//    → import { colors } from '@/constants/colors'
//    → colors.brand[500], colors.bg.base, colors.text.primary
// ─────────────────────────────────────────────

export const colors = {
  brand,
  bg,
  text,
  line,
  semantic,
  onBrand,
} as const

export type Colors = typeof colors
